services:
  - type: web
    name: getai
    env: python
    region: frankfurt
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
    autoDeploy: true
    envVars:
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings
      - key: ALLOWED_HOSTS
        value: "*"
      - key: CSRF_TRUSTED_ORIGINS_EXTRA
        value: "https://getai-uly5.onrender.com"
      # Uncomment and set if you provision Redis on Render
      # - key: REDIS_URL
      #   fromService:
      #     type: redis
      #     name: your-redis-name
      #     property: connectionString
*** End Patch```}() !!}

